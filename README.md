# Console connection demo

## Backend
Install: `npm install`

Run: `npm start`

## Frontend 
Install: `npm install`

Run: `npm dev` or `npm start`

## Running on Heroku
`git subtree push --prefix backend heroku-backend master`

`git subtree push --prefix frontend heroku-frontend master`
